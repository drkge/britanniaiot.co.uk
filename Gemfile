source "https://rubygems.org"

# Matches the exact gem versions GitHub Pages runs, so a local build and the
# deployed build cannot drift.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
